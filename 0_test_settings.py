"""
0. Тестова програма: показує всі налаштування, які відправляються до ChatGPT.

Запуск: python 0_test_settings.py
"""

import json

from openai_helper import build_chat_payload, get_chat_settings, load_env

# Пояснення кожного параметра українською (детально)
PARAM_HELP = {
    "model": (
        "Назва моделі OpenAI — головний перемикач «мозку» чату.\n"
        "  На що впливає:\n"
        "    • Якість відповідей (розуміння контексту, точність, глибина).\n"
        "    • Швидкість (легші моделі відповідають швидше).\n"
        "    • Вартість (кожен токен у дорожчих моделях коштує більше).\n"
        "    • Підтримувані можливості (деякі моделі краще працюють з кодом, таблицями тощо).\n"
        "  Типові значення:\n"
        "    • gpt-4o-mini — швидко і дешево, підходить для простих чатів і навчання.\n"
        "    • gpt-4o — якісніше, складніші питання, аналіз довгих текстів.\n"
        "    • gpt-4-turbo / інші — застаріліші варіанти, зазвичай не потрібні для цього проєкту.\n"
        "  Практика: для 1_console.py / 2_desktop.py / 3_rag.py зазвичай достатньо gpt-4o-mini. "
        "Якщо відповіді «поверхневі» — спробуйте gpt-4o."
    ),
    "messages": (
        "Повний контекст розмови — головне, що «бачить» модель перед генерацією відповіді.\n"
        "  Структура: масив об'єктів {role, content}.\n"
        "  На що впливає:\n"
        "    • system — задає правила гри: мова, стиль, обмеження («відповідай лише з контексту» у RAG).\n"
        "    • user — питання або команди людини; без них модель не знає, що відповідати.\n"
        "    • assistant — попередні відповіді бота; потрібні для діалогу з пам'яттю.\n"
        "  Важливо:\n"
        "    • Чим довший messages, тим більше токенів списується за КОЖЕН запит (вся історія пересилається знову).\n"
        "    • У 1_console.py і 2_desktop.py історія накопичується — чат «пам'ятає» розмову.\n"
        "    • У 3_rag.py у system додаються знайдені фрагменти PDF — модель відповідає з них.\n"
        "    • Порядок має значення: system зазвичай перший, далі чергуються user і assistant."
    ),
    "temperature": (
        "Контроль випадковості вибору наступного слова. Діапазон: 0.0 … 2.0.\n"
        "  Як працює: модель має список варіантів із ймовірностями; temperature «розмазує» або «стискає» ці ймовірності.\n"
        "  Значення та ефект:\n"
        "    • 0.0 — майже детерміновано: однакове питання → дуже схожі відповіді. Добре для фактів, коду, тестів.\n"
        "    • 0.3–0.5 — стримано, мало фантазії. Підходить для підтримки, інструкцій, RAG (менше «вигадок»).\n"
        "    • 0.7 (у нас за замовчуванням) — баланс: зрозуміло + трохи живості. Універсальний чат.\n"
        "    • 1.0–1.2 — креативніше: різні формулювання, ідеї, storytelling.\n"
        "    • 1.5–2.0 — дуже хаотично: можливі дивні відповіді, помилки, відхід від теми. Рідко потрібно.\n"
        "  На що впливає підвищення: більше різноманітності, менша передбачуваність, вищий ризик галюцинацій.\n"
        "  На що впливає зниження: точніше дотримання шаблону, але сухіші тексти.\n"
        "  Порада OpenAI: не крутіть одночасно temperature і top_p — змінюйте щось одне."
    ),
    "max_tokens": (
        "Жорстка стеля довжини ВІДПОВІДІ моделі (лише згенерований текст assistant, не весь запит).\n"
        "  Один токен ≈ 3–4 символи латиницею, ≈ 1–2 слова українською (залежить від тексту).\n"
        "  На що впливає:\n"
        "    • Маленьке значення (50–150) — короткі відповіді, може обірватися на півслові.\n"
        "    • 500–1000 (у нас 1000) — звичайний абзац або кілька; достатньо для більшості питань.\n"
        "    • 2000–4000 — довгі пояснення, есе, розгорнутий код.\n"
        "    • Дуже велике — дорожче (плата за вихідні токени) і довше чекати.\n"
        "  Якщо відповідь обривається — збільшіть max_tokens.\n"
        "  Якщо модель «лиє воду» — зменшіть max_tokens або уточніть у system: «відповідай коротко».\n"
        "  Не плутати з довжиною messages: max_tokens обмежує лише нову відповідь, не історію чату."
    ),
    "top_p": (
        "Nucleus sampling (top-p): альтернатива temperature для контролю різноманітності. Діапазон: 0.0 … 1.0.\n"
        "  Як працює: модель сортує варіанти за ймовірністю і бере лише ті, чия сумарна ймовірність ≤ top_p.\n"
        "  Значення та ефект:\n"
        "    • 1.0 (у нас за замовчуванням) — розглядаються всі варіанти; top_p фактично вимкнений.\n"
        "    • 0.9 — легке обрізання малоймовірних «дивних» слів.\n"
        "    • 0.7–0.8 — відповіді стабільніші, менше сюрпризів.\n"
        "    • 0.1–0.3 — дуже вузький вибір, майже як при низькій temperature.\n"
        "  На що впливає зниження: менше креативності, більше «безпечних» формулювань.\n"
        "  Взаємодія з temperature: обидва параметри керують випадковістю. "
        "Якщо temperature=0.7 і top_p=1.0 — керує переважно temperature."
    ),
    "frequency_penalty": (
        "Штраф за ПОВТОРЕННЯ ТОКЕНІВ, які вже з'явилися у відповіді. Діапазон: -2.0 … 2.0.\n"
        "  Як працює: чим частіше слово вже використане в поточній відповіді, тим менша ймовірність повторити його знову.\n"
        "  Значення та ефект:\n"
        "    • 0.0 (у нас) — без штрафу; модель може повторювати слова природно.\n"
        "    • 0.3–0.7 — помірно зменшує «так, так, так» і зациклення на одному слові.\n"
        "    • 1.0–2.0 — сильно уникає повторів; текст може стати незвичним, синоніми замість нормальних повторів.\n"
        "    • Від'ємні значення — навпаки схиляють до повторень (майже не використовують).\n"
        "  Коли збільшувати: списки без дублювання слів, вірші, коли бот повторює одне й те саме.\n"
        "  Відмінність від presence_penalty: frequency дивиться на ЧАСТОТУ слова в відповіді, "
        "а не на те, чи згадувалась тема взагалі."
    ),
    "presence_penalty": (
        "Штраф за повторне згадування ТЕМ, які вже були в відповіді (на рівні «був чи не був токен»). "
        "Діапазон: -2.0 … 2.0.\n"
        "  Як працює: якщо слово/тема вже з'явилися хоча б раз, наступні згадування отримують штраф.\n"
        "  Значення та ефект:\n"
        "    • 0.0 (у нас) — модель вільно повертається до тієї ж теми.\n"
        "    • 0.3–0.6 — заохочує розширювати відповідь новими аспектами, а не перефразовувати те саме.\n"
        "    • 1.0–2.0 — сильний поштовх до нових тем; ризик відходу від питання.\n"
        "  Коли збільшувати: brainstorming, ідеї, коли потрібно «розігнати» однотипні відповіді.\n"
        "  Для RAG (3_rag.py) зазвичай залишають 0.0 — важливо чітко відповісти по суті, навіть якщо слова повторюються.\n"
        "  frequency vs presence: frequency — «не пиши це слово вдесяте», presence — «не повертайся до цієї теми знову»."
    ),
    "stream": (
        "Режим доставки відповіді: потоково (stream=true) або одним шматком (stream=false).\n"
        "  stream=false (у наших програмах):\n"
        "    • Чекаємо повну відповідь, потім показуємо її цілком.\n"
        "    • Простіший код — один JSON з полем choices[0].message.content.\n"
        "  stream=true:\n"
        "    • Текст з'являється по частинах (як у ChatGPT у браузері).\n"
        "    • Складніша обробка: багато маленьких фрагментів (SSE/chunked).\n"
        "    • Краще для UX у великих застосунках; на параметри якості відповіді не впливає.\n"
        "  На зміст відповіді не впливає — лише на те, КОЛИ ви бачите текст."
    ),
}

MESSAGE_ROLE_HELP = {
    "system": (
        "Роль «режисера» для моделі. Не показується користувачу як повідомлення чату, але має високий пріоритет.\n"
        "      Тут задають: мову (українською), тон (коротко / формально), правила (не вигадуй факти).\n"
        "      У 3_rag.py сюди ж підставляється знайдений контекст з PDF — модель «читає» його перед відповіддю.\n"
        "      Зміна system змінює поведінку ВСЬОГО чату, навіть без зміни temperature."
    ),
    "user": (
        "Повідомлення людини: питання, уточнення, команди.\n"
        "      Саме на останнє user-повідомлення модель генерує наступну відповідь.\n"
        "      Чітке питання → точніша відповідь; розмите → загальна відповідь."
    ),
    "assistant": (
        "Попередні відповіді моделі в цій сесії.\n"
        "      Потрібні, щоб працювали фрази на кшталт «поясни простіше» або «а тепер приклад до цього».\n"
        "      Кожен новий запит у 1_console.py / 2_desktop.py надсилає ВСЮ історію — це збільшує вартість і час обробки."
    ),
}


def print_separator(title: str = "") -> None:
    print("\n" + "=" * 60)
    if title:
        print(title)
        print("=" * 60)


def print_help_block(text: str, indent: str = "  ") -> None:
    for line in text.split("\n"):
        print(f"{indent}{line}")


def main() -> None:
    load_env()

    print_separator("НАЛАШТУВАННЯ З .env")
    settings = get_chat_settings()
    for key, value in settings.items():
        print(f"  {key}: {value}")

    # Приклад повідомлень, як у реальному чаті
    example_messages = [
        {"role": "system", "content": "Ти корисний помічник. Відповідай українською."},
        {"role": "user", "content": "Що таке Python?"},
    ]

    payload = build_chat_payload(example_messages)

    print_separator("ТІЛО ЗАПИТУ (JSON), ЯКЕ ВІДПРАВЛЯЄТЬСЯ НА API")
    print(json.dumps(payload, ensure_ascii=False, indent=2))

    print_separator("ПОЯСНЕННЯ КОЖНОГО ПАРАМЕТРА")
    for key, value in payload.items():
        help_text = PARAM_HELP.get(key, "Додатковий параметр API.")
        print(f"\n▸ {key}")
        print(
            f"  Значення: {json.dumps(value, ensure_ascii=False) if not isinstance(value, list) else '(див. розділ messages нижче)'}"
        )
        print("  Пояснення:")
        print_help_block(help_text, indent="    ")

    print_separator("СТРУКТУРА messages (приклад діалогу)")
    for i, msg in enumerate(example_messages, 1):
        role = msg["role"]
        print(f"\n  Повідомлення {i}:")
        print(f"    role: {role}")
        print("    Пояснення:")
        print_help_block(MESSAGE_ROLE_HELP.get(role, ""), indent="      ")
        print(f"    content: {msg['content']}")

    print_separator("HTTP-ЗАГОЛОВКИ ЗАПИТУ")
    print("  Authorization: Bearer <OPENAI_API_KEY>")
    print("    Ключ API з .env. Ніколи не публікуйте його в коді чи git.")
    print("  Content-Type: application/json")
    print("    Тіло запиту передається у форматі JSON.")

    print_separator("URL API")
    print("  POST https://api.openai.com/v1/chat/completions")
    print("\nЦя програма НЕ надсилає запит — лише показує, що було б відправлено.")
    print("Для реального чату запустіть: python 1_console.py\n")


if __name__ == "__main__":
    main()
